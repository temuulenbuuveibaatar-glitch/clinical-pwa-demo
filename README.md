## Fullstack demo added
A basic backend (Express), CouchDB service via docker-compose, admin UI placeholder, medication CSV import script, and seed users were added in demo branch. Start with `docker-compose up --build` to bring up CouchDB and the API.
